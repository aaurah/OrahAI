/**
 * DeploymentService — Phase 2 placeholder
 * Deployments are not part of MVP. This file is a stub so imports compile.
 */
export class DeploymentService {
  async deploy(_projectId: string): Promise<never> {
    throw Object.assign(new Error("Deployments not yet available (Phase 2)"), { statusCode: 501 });
  }
}
